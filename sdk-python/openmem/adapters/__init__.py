"""OMP adapter implementations.

Adapters translate the OMP standard verbs to a specific backend's API
(translation adapter) or proxy directly to a provider that speaks OMP
natively (passthrough adapter).
"""
